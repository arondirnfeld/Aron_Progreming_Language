# בדיקת לקסר (tokenizer)

# 1. ביטויים פשוטים
הדפס "שלום עולם"
קבע מספר = 42

# 2. ביטויים עם סוגריים
קבע תוצאה = (5 + 3) * 2

# 3. ערכים בוליאנים
קבע אמיתי = אמת
קבע שקרי = שקר

# 4. משפטי תנאי
אם 5 > 3
    הדפס "חמש גדול משלוש"
סוף
